{
  "name": "stellar-passkey-ui",
  "version": "0.0.0",
  "private": true,
  "description": "A minimal, composable passkey SDK and UI components for Stellar smart wallets, built around a maintained cross-platform compatibility guide.",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.28.2",
  "scripts": {
    "build": "pnpm -r --filter \"./packages/*\" run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:dist": "node scripts/dist-smoke.mjs",
    "test:e2e": "pnpm --filter @passkey-ui/e2e run test",
    "typecheck": "pnpm -r run typecheck",
    "lint": "biome check .",
    "format": "biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
