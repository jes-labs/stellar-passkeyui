# Stellar Passkey Compatibility Guide

> Generated from `packages/compat/src/data`. Do not edit this file by hand — change the data and regenerate.

Data as of **2026-06-12**.

This guide records where passkey flows work across browsers, operating systems, authenticators, and embedding contexts, why they break when they do, and the fallback a wallet should take. The SDK reads the same dataset to choose its runtime fallbacks, so the guidance here and the code cannot drift apart.

## How to read this

Every finding carries a verification status:

- **documented** — sourced from specifications, vendor documentation, or issue trackers, and not yet re-confirmed on real hardware by this project.
- **automated** — exercised by the CI virtual-authenticator harness.
- **manual-device** — confirmed by hand on a named real device.

Outcomes are one of ✅ works, ⚠️ partial, ❌ fails, or ⛔ unsupported. A last-verified date sits on every entry so you can see what is fresh.

## Transport security

### WebAuthn is unavailable on insecure (HTTP) origins

⛔ unsupported · feature: `credential-get` · documented, verified 2026-06-11

**Applies to:** chrome, safari, firefox, edge, chrome-android, safari-ios

**What happens:** navigator.credentials requires a secure context. Plain HTTP origins other than localhost do not expose the API at all.

**Fallback:** Serve the wallet over HTTPS. localhost and 127.0.0.1 are treated as secure contexts for development.

**Recommendation:** Hard-require HTTPS in production and fail early with a clear message when the page is served over HTTP.

**SDK rule:** on `insecure-context`, take `require-secure-context`.

Sources:
- https://developer.mozilla.org/en-US/docs/Web/Security/Secure_Contexts
- https://www.w3.org/TR/webauthn-2/#sctn-secure-contexts

## Embedding context

### Safari blocks passkey creation inside cross-origin iframes

❌ fails · feature: `credential-create` · documented, verified 2026-06-11

**Applies to:** safari, safari-ios on macos, ios

**What happens:** WebKit does not expose navigator.credentials.create() to cross-origin iframes, so an embedded registration call is rejected.

**Fallback:** Run the create flow in a popup or full-page redirect on the wallet origin, then return to the embedding page.

**Recommendation:** Detect Safari and route passkey creation out of the iframe to a popup; a get() ceremony can stay in the frame where the permissions policy allows it.

**SDK rule:** on `safari-create-in-iframe`, take `open-popup-for-create`.

Sources:
- https://github.com/WebKit/standards-positions/issues/304
- https://developer.apple.com/documentation/authenticationservices

### Cross-origin iframes need an explicit permissions policy for WebAuthn

⚠️ partial · feature: `credential-get` · documented, verified 2026-06-11

**Applies to:** chrome, edge, chrome-android, firefox, safari

**What happens:** Without "publickey-credentials-get" and "publickey-credentials-create" granted to the wallet origin in the iframe allow attribute, browsers block WebAuthn calls made from the frame.

**Fallback:** Set the iframe allow attribute to grant both permissions to the wallet origin. When the embedding page is not under your control, fall back to a popup.

**Recommendation:** Ship the correct allow attribute as part of the embed snippet and document it; treat a missing or wrong policy as a popup trigger rather than a hard failure.

**SDK rule:** on `cross-origin-iframe`, take `set-iframe-permissions-or-popup`.

Sources:
- https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Permissions-Policy/publickey-credentials-get
- https://www.w3.org/TR/webauthn-2/#sctn-permissions-policy

## Authenticator

### A user-verifying platform authenticator may not be present

⚠️ partial · feature: `credential-create` · documented, verified 2026-06-11

**Applies to:** chrome, firefox, edge on windows, linux

**What happens:** A desktop without Windows Hello configured, or a Linux machine without a platform authenticator, reports no user-verifying platform authenticator; isUserVerifyingPlatformAuthenticatorAvailable() returns false.

**Fallback:** Offer a roaming security key or the cross-device (phone) flow when no platform authenticator is available.

**Recommendation:** Probe isUserVerifyingPlatformAuthenticatorAvailable() before offering "create on this device"; otherwise present cross-device or security-key options.

**SDK rule:** on `no-platform-authenticator`, take `offer-cross-device-or-security-key`.

Sources:
- https://developer.mozilla.org/en-US/docs/Web/API/PublicKeyCredential/isUserVerifyingPlatformAuthenticatorAvailable_static
- https://passkeys.dev/device-support/

## Credential storage

### Synced and device-bound passkeys behave differently for recovery

✅ works · feature: `discoverability` · documented, verified 2026-06-11

**Applies to:** safari, chrome, safari-ios, chrome-android on ios, android, macos

**What happens:** iCloud Keychain and Google Password Manager sync passkeys across a user’s devices, while roaming security keys and some enterprise configurations are device-bound and do not sync.

**Fallback:** Do not assume a passkey survives device loss. For device-bound setups, require enrolling a second signer up front.

**Recommendation:** Surface whether a credential is synced, and for device-bound credentials push the user to add a recovery signer during onboarding.

Sources:
- https://passkeys.dev/docs/reference/terms/
- https://support.apple.com/en-us/102195

## Cross-device

### Cross-device sign-in lets a phone authenticate a desktop

✅ works · feature: `cross-device-auth` · documented, verified 2026-06-11

**Applies to:** chrome, edge, safari on macos, windows, android, ios

**What happens:** The hybrid transport pairs a desktop browser with a phone over a QR code and Bluetooth proximity check; it is supported across Chromium and Safari, with UX differences between them.

**Fallback:** When no local authenticator fits, present the cross-device option; it requires Bluetooth on both ends.

**Recommendation:** Offer cross-device as a first-class path for desktops without a platform authenticator, and explain the Bluetooth requirement.

Sources:
- https://passkeys.dev/docs/reference/terms/
- https://developers.google.com/identity/passkeys/supported-environments

## Capability flags

### Discoverable (resident) credential support varies by authenticator

⚠️ partial · feature: `resident-key` · documented, verified 2026-06-11

**Applies to:** chrome, safari, firefox, edge

**What happens:** Platform authenticators such as iCloud Keychain, Google Password Manager, and Windows Hello support discoverable credentials, but some roaming security keys have limited resident-key storage or none.

**Fallback:** When resident keys are unavailable, fall back to non-discoverable credentials and persist the credential ID so it can be passed in allowCredentials on sign-in.

**Recommendation:** Request residentKey "preferred" unless usernameless login is essential, and always store the credential ID so sign-in works either way.

**SDK rule:** on `no-resident-key`, take `use-credential-id-allowlist`.

Sources:
- https://passkeys.dev/device-support/
- https://www.w3.org/TR/webauthn-2/#dom-authenticatorselectioncriteria-residentkey

### The passkey-to-address discoverability problem

⚠️ partial · feature: `discoverability` · documented, verified 2026-06-11

**Applies to:** all platforms

**What happens:** The smart-wallet address derives from the passkey public key, so the address is unknown until after the credential exists, yet WebAuthn does not let you rename a credential afterwards to carry that address.

**Fallback:** Record the credential-ID to contract-address mapping at creation time (in the indexer and locally), and use discoverable credentials where supported so the credential can be found without knowing the address first.

**Recommendation:** Persist the mapping on create and treat the indexer as the lookup of record. Do not rely on naming the passkey with the address.

Sources:
- https://porto.sh
- https://passkeys.dev/docs/use-cases/bootstrapping/

### userVerification "required" can break some authenticators

⚠️ partial · feature: `user-verification` · documented, verified 2026-06-11

**Applies to:** chrome, safari, firefox, edge

**What happens:** Forcing userVerification "required" demands a biometric or PIN. A roaming key without a configured PIN, or certain platform states, rejects the ceremony rather than degrading to a lower assurance level.

**Fallback:** Use "preferred" for broad compatibility and reserve "required" for flows whose security model genuinely needs guaranteed user verification.

**Recommendation:** Default to "preferred" and make "required" an explicit, documented opt-in chosen per wallet policy.

Sources:
- https://www.w3.org/TR/webauthn-2/#enum-userVerificationRequirement
- https://passkeys.dev/docs/reference/terms/

### Passkey autofill (conditional UI) is not universally available

⚠️ partial · feature: `conditional-mediation` · documented, verified 2026-06-12

**Applies to:** chrome, safari, edge, chrome-android, safari-ios

**What happens:** mediation "conditional" drives passkey autofill in form fields and is gated behind PublicKeyCredential.isConditionalMediationAvailable(); older browsers and Firefox lag behind.

**Fallback:** Feature-detect conditional mediation; when it is absent, show an explicit "Sign in with a passkey" button instead of relying on autofill.

**Recommendation:** Always offer the explicit button and layer autofill on top only where it is available.

**SDK rule:** on `no-conditional-mediation`, take `show-explicit-passkey-button`.

Sources:
- https://web.dev/articles/passkey-form-autofill
- https://developer.mozilla.org/en-US/docs/Web/API/PublicKeyCredential/isConditionalMediationAvailable_static

### Request no attestation for consumer passkey wallets

✅ works · feature: `attestation` · documented, verified 2026-06-11

**Applies to:** chrome, safari, firefox, edge

**What happens:** Direct or enterprise attestation can surface extra consent prompts and privacy concerns, and platform authenticators frequently return none regardless.

**Fallback:** Use attestation "none" and do not depend on attestation data for the wallet trust model.

**Recommendation:** Default attestation to "none" unless a specific compliance requirement dictates otherwise.

Sources:
- https://www.w3.org/TR/webauthn-2/#enum-attestation-convey
- https://passkeys.dev/docs/advanced/

## Verified device sessions

Real-hardware tests of the reference demo. Each entry records only what the tester actually confirmed.

| Date | Device | Browser | Authenticator | Outcome | Confirmed |
| ---- | ------ | ------- | ------------- | ------- | --------- |
| 2026-06-11 | MacBook Pro (macos) | Brave | Touch ID | ✅ works | Passkey created and a transaction payload signed end to end. Note: Brave reports a Chrome user agent; the brand is only visible through client hints. |
| 2026-06-12 | MacBook Pro (macos) | Safari | Touch ID | ✅ works | Passkey created and a transaction payload signed on the deployed demo. |
| 2026-06-12 | iPhone (ios) | Safari | Face ID | ✅ works | Face ID prompt completed and the passkey flow finished on the deployed demo. |
| 2026-06-12 | Android phone (android) | Chrome | Fingerprint | ✅ works | Passkey flow completed on the deployed demo. |
| 2026-06-12 | Android phone (android) | Opera Mini | Fingerprint | ✅ works | Passkey flow completed on the deployed demo. Opera on Android runs on the Blink engine, which matches the behavior the matrix predicts for Chromium-family browsers. |
| 2026-06-12 | MacBook Pro (macos) | Firefox | Touch ID | ✅ works | Passkey flow completed on the deployed demo. All capability lights on, including conditional mediation. |
| 2026-06-12 | Android phone (android) | Firefox | Fingerprint | ⚠️ partial | Create and sign completed, but conditional mediation (passkey autofill) is unavailable; the SDK surfaced the documented advisory and fell back to the explicit button. First on-device confirmation of the conditional-mediation finding. |
| 2026-06-12 | Android phone (android) | Edge | Fingerprint | ✅ works | Passkey flow completed on the deployed demo. |

## Maintenance

The matrix lives as structured data and is regenerated, not edited. Entries marked **documented** are awaiting confirmation by the automated harness (virtual authenticators in CI) and the manual device lab; as those run, entries move to **automated** or **manual-device** and their dates advance. An entry that has not been re-verified within the review window is a signal to re-check it, not a guarantee it still holds.
