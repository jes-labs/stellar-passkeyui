{
  "name": "example-kit-integration",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Reference integration: the passkey module running inside the real Stellar Wallets Kit. Consumes the kit built from the feat/passkey-module branch; see README.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "@creit.tech/stellar-wallets-kit": "file:../../../Stellar-Wallets-Kit/src/dist",
    "@stellar/stellar-base": "^14.1.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^5.4.0"
  }
}
